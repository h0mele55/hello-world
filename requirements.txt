# Core (Stage 0-1) — needed to run the rating engine and tests
SQLAlchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0

# API + Telegram channel (Stage 0-3)
fastapi>=0.110
uvicorn[standard]>=0.29
python-telegram-bot>=21

# Scheduler (Stage 3)
APScheduler>=3.10
tzdata>=2024.1

# Rate-table ingestion (Stage 2/4)
pandas>=2.0
openpyxl>=3.1
pdfplumber>=0.11
PyYAML>=6.0

# Migrations
alembic>=1.13

# Dev/test
pytest>=8.0
